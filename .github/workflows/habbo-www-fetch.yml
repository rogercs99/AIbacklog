name: Habbo R39 WWW fetch
on:
  workflow_dispatch:
jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Full WWW fetch disabled by default; critical files are already preserved.'
